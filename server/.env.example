# Example .env file for the promptlab server
# Put real values in a real .env (do NOT commit secrets to version control)
GEMINI_API_KEY=
# Configure to the REST endpoint for your Gemini model; if omitted the server uses a local fallback
GEMINI_API_URL=
PORT=3000
